import React from 'react';
import Menu from './Menu';

const Contact = () => {
    return (
        <div>
            <input type="text" />
            <button type="submit">Submit</button>
        </div>
    );
};

export default Contact;