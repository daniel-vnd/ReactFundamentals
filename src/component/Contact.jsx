import React from 'react';

const Contact = () => {
    return (
        <div>
            <input type="text" />
            <button type="submit">Submit</button>
        </div>
    );
};

export default Contact;